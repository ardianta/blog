image: registry.gitlab.com/pages/hugo:latest

before_script:
  - apk add --update --no-cache git
  - hugo version

pages:
  script:
  - hugo --minify --enableGitInfo
  artifacts:
    paths:
    - public
  only:
  - master